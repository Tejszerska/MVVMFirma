<UserControl x:Class="MVVMFirma.Views.AllClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="client_id"/>
                <DataGridTextColumn Header="first_name"/>
                <DataGridTextColumn Header="last_name"/>
                <DataGridTextColumn Header="document_type"/>
                <DataGridTextColumn Header="document_number"/>
                <DataGridTextColumn Header="pesel"/>
                <DataGridTextColumn Header="address"/>
                <DataGridTextColumn Header="address_source"/>
                <DataGridTextColumn Header="phone"/>
                <DataGridTextColumn Header="email"/>
                <DataGridTextColumn Header="is_active"/>
                <DataGridTextColumn Header="history_id"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
