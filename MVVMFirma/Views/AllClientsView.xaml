<local:AllViewBase x:Class="MVVMFirma.Views.AllClientsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Client ID"  Binding="{Binding client_id}"/>
                <DataGridTextColumn Header="First name" Binding="{Binding first_name}"/>
                <DataGridTextColumn Header="Last name" Binding="{Binding last_name}"/>
                <DataGridTextColumn Header="Document type" Binding="{Binding document_type}"/>
                <DataGridTextColumn Header="Document number" Binding="{Binding document_number}"/>
                <DataGridTextColumn Header="PESEL" Binding="{Binding pesel}"/>
                <DataGridTextColumn Header="Address" Binding="{Binding address}"/>
                <DataGridTextColumn Header="Address source" Binding="{Binding address_source}"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding phone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding email}"/>
                <DataGridTextColumn Header="Active?" Binding="{Binding is_active}"/>
                <DataGridTextColumn Header="History ID" Binding="{Binding history_id}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
