<local:AllViewBase x:Class="MVVMFirma.Views.AllPawnLoanItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Pawn loan item ID" Binding="{Binding pawn_loan_item_id}"/>
                <DataGridTextColumn Header="Pawn loan ID" Binding="{Binding pawn_loan_id}"/>
                <DataGridTextColumn Header="Item ID" Binding="{Binding item_id}"/>
                <DataGridTextColumn Header="Loan amount for item" Binding="{Binding loan_amount_for_item}"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding notes}"/>
                <DataGridTextColumn Header="History ID" Binding="{Binding history_id}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
