<local:AllViewBase x:Class="MVVMFirma.Views.RecordsHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="RecordHistoryID" Binding="{Binding RecordHistoryID}"/>
                <DataGridTextColumn Header="CreateEmployee" Binding="{Binding CreateEmployee}"/>
                <DataGridTextColumn Header="CreateDate" Binding="{Binding CreateDate}"/>
                <DataGridTextColumn Header="CreateBranch" Binding="{Binding CreateBranch}"/>
                <DataGridTextColumn Header="ModifyEmployee" Binding="{Binding ModifyEmployee}"/>
                <DataGridTextColumn Header="ModifyDate" Binding="{Binding ModifyDate}"/>
                <DataGridTextColumn Header="DeleteEmployee" Binding="{Binding DeleteEmployee}"/>
                <DataGridTextColumn Header="DeleteDate" Binding="{Binding DeleteDate}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
