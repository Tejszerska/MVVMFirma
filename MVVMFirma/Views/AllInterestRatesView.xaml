<UserControl x:Class="MVVMFirma.Views.AllInterestRates"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="interest_rate_id"/>
                <DataGridTextColumn Header="name"/>
                <DataGridTextColumn Header="rate_percent"/>
                <DataGridTextColumn Header="period_days"/>
                <DataGridTextColumn Header="minimal_interest"/>
                <DataGridTextColumn Header="is_default"/>
                <DataGridTextColumn Header="is_active"/>
                <DataGridTextColumn Header="history_id"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
