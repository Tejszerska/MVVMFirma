<local:AllViewBase x:Class="MVVMFirma.Views.AllPriceHistory"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Item ID" Binding="{Binding ItemID}"/>
                <DataGridTextColumn Header="Item Name" Binding="{Binding ItemName}"/>
                <DataGridTextColumn Header="Change Date" Binding="{Binding ChangeDate, StringFormat=\{0:yyyy-MM-dd\}}"/>
                <DataGridTextColumn Header="Old Price" Binding="{Binding OldPrice}"/>
                <DataGridTextColumn Header="New Price" Binding="{Binding NewPrice}"/>
                <DataGridTextColumn Header="Changed By Employee" Binding="{Binding ChangedByEmployee}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
