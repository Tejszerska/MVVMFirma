<local:AllViewBase x:Class="MVVMFirma.Views.AllSalesItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
        <DataGrid.Columns>
                <DataGridTextColumn Header="sales_item_id" Binding="{Binding sales_item_id}"/>
                <DataGridTextColumn Header="sale_id" Binding="{Binding sale_id}"/>
                <DataGridTextColumn Header="item_id" Binding="{Binding item_id}"/>
                <DataGridTextColumn Header="sold_price_for_item" Binding="{Binding sold_price_for_item}"/>
                <DataGridTextColumn Header="history_id" Binding="{Binding history_id}"/>
        </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
