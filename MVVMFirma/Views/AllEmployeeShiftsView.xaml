<local:AllViewBase x:Class="MVVMFirma.Views.AllEmployeeShiftsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Shift ID" Binding="{Binding shift_id}"/>
                <DataGridTextColumn Header="Employee ID" Binding="{Binding employee_id}"/>
                <DataGridTextColumn Header="Branch ID" Binding="{Binding branch_id}"/>
                <DataGridTextColumn Header="Shift start" Binding="{Binding shift_start}"/>
                <DataGridTextColumn Header="Shift end" Binding="{Binding shift_end}"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding notes}"/>
                <DataGridTextColumn Header="History ID" Binding="{Binding history_id}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
