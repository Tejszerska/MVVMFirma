<local:AllViewBase x:Class="MVVMFirma.Views.AllEmployeeShiftsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Shift ID" Binding="{Binding EmployeeShiftId}"/>
                <DataGridTextColumn Header="First Name" Binding="{Binding EmployeeFirstName}"/>
                <DataGridTextColumn Header="Last Name" Binding="{Binding EmployeeLastName}"/>
                <DataGridTextColumn Header="Branch Name" Binding="{Binding BranchName}"/>
                <DataGridTextColumn Header="Shift start" Binding="{Binding ShiftStart}"/>
                <DataGridTextColumn Header="Shift end" Binding="{Binding ShiftEnd}"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
