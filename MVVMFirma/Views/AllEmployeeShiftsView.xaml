<UserControl x:Class="MVVMFirma.Views.AllEmployeeShiftsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="shift_id"/>
                <DataGridTextColumn Header="employee_id"/>
                <DataGridTextColumn Header="branch_id"/>
                <DataGridTextColumn Header="shift_start"/>
                <DataGridTextColumn Header="shift_end"/>
                <DataGridTextColumn Header="notes"/>
                <DataGridTextColumn Header="history_id"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
