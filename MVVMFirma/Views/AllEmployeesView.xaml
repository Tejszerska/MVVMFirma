<local:AllViewBase x:Class="MVVMFirma.Views.AllEmployeesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVMFirma.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <DataGrid AutoGenerateColumns="False" ItemsSource="{Binding List}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee ID" Binding="{Binding employee_id}"/>
                <DataGridTextColumn Header="First name" Binding="{Binding first_name}"/>
                <DataGridTextColumn Header="Last name" Binding="{Binding last_name}"/>
                <DataGridTextColumn Header="Login" Binding="{Binding login}"/>
                <DataGridTextColumn Header="Role" Binding="{Binding employee_role_id}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</local:AllViewBase>
